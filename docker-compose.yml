# EndoScan — docker-compose (PLACEHOLDER / STUB).
#
# This file is intentionally a stub at M0. It defines NO working services yet.
# The `api` and `web` service blocks below are commented out and will be wired
# up in later milestones (FastAPI service: ~M7; web frontend: ~M8). Do not
# uncomment until those milestones implement the corresponding services.

# services:
#   api:
#     # TODO(M7): FastAPI service — thin; imports endoscan_core.
#     build: ./services/api
#     ports:
#       - "8000:8000"
#     env_file:
#       - .env
#
#   web:
#     # TODO(M8): React / Vite / Tailwind / TypeScript frontend.
#     build: ./apps/web
#     ports:
#       - "5173:5173"
