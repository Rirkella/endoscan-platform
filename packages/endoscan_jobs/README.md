# EndoScan job runner

Containerized heavy-job entrypoint (`python -m endoscan_jobs run <job>`). Science lives
in `endoscan_core`; this package holds the CLI, runner, storage interface, reviewed
heavy-data extraction, coverage, identity, fusion, and Explore-map jobs.
