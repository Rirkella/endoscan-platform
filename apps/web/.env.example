# Base URL for the EndoScan serving API. In local dev this is unset and the Vite proxy
# forwards same-origin /api -> http://127.0.0.1:8000. In a deployed build, set this to the
# API's public base (e.g. https://api.example.org). No trailing slash.
VITE_API_BASE_URL=
